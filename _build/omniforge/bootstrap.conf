#!/usr/bin/env bash
# =============================================================================
# bootstrap.conf (deprecated)
# =============================================================================
# This file is no longer used by OmniForge at runtime.
# All configuration now lives in:
#   - omni.config          (Section 1 & top-level settings)
#   - omni.profiles.sh     (profile data)
#   - omni.phases.sh       (phase metadata)
#   - omni.settings.sh     (advanced/system settings, derived values)
# =============================================================================
