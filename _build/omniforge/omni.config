#!/usr/bin/env bash
# =============================================================================
# OmniForge Config Overlay (omni.config)
# =============================================================================
# This file mirrors SECTION 1: QUICK START from bootstrap.conf and can override
# its defaults. It is loaded after bootstrap.conf by lib/bootstrap.sh.
# Precedence:
#   1) Environment variables (if set) override values below.
#   2) omni.config overrides defaults from bootstrap.conf.
#   3) bootstrap.conf provides baseline defaults.
#
# Future refactor will:
#   - Switch loaders to source omni.config
#   - Turn bootstrap.conf into a thin shim or remove it.
# =============================================================================

#!/usr/bin/env bash
# =============================================================================
# =============================================================================
# SECTION 1: QUICK START OVERRIDES (mirror from bootstrap.conf)
# =============================================================================

# SECTION 1: QUICK START - USER CONFIGURABLE
# =============================================================================
# These are the most commonly changed settings for new projects

# --- Project Identity ---
APP_NAME="Bloom"
APP_VERSION="0.1.0"
APP_DESCRIPTION="Built with OmniForge"

# --- Installation Target ---
# Options: "test" (installs to ./test/install-1) or "prod" (installs to ./app)
INSTALL_TARGET="test"

# --- Stack Profile ---
# Options: ai_automation, fpa_dashboard, collab_editor, erp_gateway, asset_manager, custom_bos
# TODO: Use `omni --config` for interactive profile editor (coming soon)
STACK_PROFILE="asset_manager"

# --- Database Configuration ---
DB_NAME="BloomDB"
DB_USER="luce"
DB_PASSWORD="r6VcpsjgqngnXV"
DB_HOST="localhost"
DB_PORT="5432"

# --- Feature Flags ---
# Enable/disable major technology stacks (can be overridden by profile)
ENABLE_AUTHJS="true"
ENABLE_AI_SDK="true"
ENABLE_PG_BOSS="true"
ENABLE_SHADCN="true"
ENABLE_ZUSTAND="true"
ENABLE_PDF_EXPORTS="true"
ENABLE_TEST_INFRA="true"
ENABLE_CODE_QUALITY="true"


# =============================================================================
