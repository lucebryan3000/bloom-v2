#!/usr/bin/env bash
# =============================================================================
# OmniForge Config (omni.config)
# =============================================================================
# Canonical Section 1 (QUICK START) settings.
# Loaded by lib/bootstrap.sh after bootstrap.conf; precedence:
#   1) Environment variables (if set) override values below.
#   2) omni.config provides Section 1 values.
#   3) bootstrap.conf still loads for other domains (profiles, phases, advanced)
#      but is no longer authoritative for Section 1.
# =============================================================================
# SECTION 1: QUICK START - USER CONFIGURABLE
# =============================================================================

# SECTION 1: QUICK START - USER CONFIGURABLE
# =============================================================================
# These are the most commonly changed settings for new projects

# --- Project Identity ---
APP_NAME="Bloom"
APP_VERSION="0.1.0"
APP_DESCRIPTION="Built with OmniForge"

# --- Installation Target ---
# Options: "test" (installs to ./test/install-1) or "prod" (installs to ./app)
INSTALL_TARGET="test"

# --- Stack Profile ---
# Options: ai_automation, fpa_dashboard, collab_editor, erp_gateway, asset_manager, custom_bos
# TODO: Use `omni --config` for interactive profile editor (coming soon)
STACK_PROFILE="asset_manager"

# --- Database Configuration ---
DB_NAME="BloomDB"
DB_USER="luce"
DB_PASSWORD=""
DB_HOST="localhost"
DB_PORT="5432"

# --- Feature Flags ---
# Enable/disable major technology stacks (can be overridden by profile)
ENABLE_AUTHJS="true"
ENABLE_AI_SDK="true"
ENABLE_PG_BOSS="true"
ENABLE_SHADCN="true"
ENABLE_ZUSTAND="true"
ENABLE_PDF_EXPORTS="true"
ENABLE_TEST_INFRA="true"
ENABLE_CODE_QUALITY="true"


# =============================================================================
